#include "board.h"

gameBoard::gameBoard()
{
  //populate this vector with blocks for the gameboard, assign them the appropriate bmp texture file
}

gameBoard::~gameBoard()
{
 //clear vector (although C++ auto-clears it IIRC)
}
